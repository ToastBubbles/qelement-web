import { useNavigate } from "react-router";
import { Link } from "react-router-dom";

export default function SearchBarMain() {
  return <input className="main-searchbar" type="text" placeholder="Search" />;
}
